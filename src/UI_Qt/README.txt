Install instructions:

1. Create these symlinks:
  ln -s <PATH TO YOU CUR ZYN ABS BRANCH> abs
  ln -s <PATH TO A RECENT LMMS REPOSITORY> lmms_include
2. mkdir build && cd build
3. cmake ../
4. make


